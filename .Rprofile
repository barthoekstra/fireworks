source("renv/activate.R")
if (Sys.info()["sysname"] != "Darwin") {
  .libPaths(c("/data/volume_2/R/"))  
}
# if (Sys.readlink("data") == "" | is.na(Sys.readlink("data"))) {
#   file.symlink("/mnt/fireworks/", "data")  
# }
